# Credentials — never commit
creds/

# Downloaded API responses (cache)
.hsp-cache/

# Live-capture working dir (used when refreshing recorded fixtures)
live-capture/

# Bulk local ticket dumps (multi-MB phone photos) — do not commit
sample-tickets/

# Runtime ticket workflow tree (intake / processed / claimed) — repo root only
/tickets/

# Python
__pycache__/
*.pyc
.pytest_cache/

# IDE
.idea/workspace.xml
.idea/copilot.data.migration.agent.xml

# CLI claim output
Results/

_dry_signoff/

