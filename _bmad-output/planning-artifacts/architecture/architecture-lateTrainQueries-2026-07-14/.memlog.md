---
scope: Late Train Query Engine — module boundaries, migration approach, cancellation seam
purpose: Build substrate to feed epics/stories
altitude: feature (MVP of a solo Python CLI)
updated: 2026-07-14T14:29
---

- (direction) Working mode = Coaching path
- (decision) Migration = FULL GREENFIELD REWRITE (Simon's call, overrides my lean of rewrite-core-reuse-IO). Rationale: clean ports/adapters slate serves the Lambda endgame + roadmap 'clean up/break down' better than refactoring the 225-line monolith. Prevents: carrying inverted old-model assumptions and rough code forward.
- (constraint) Greenfield must re-implement (not lose) NFR3 AVG-TLS REQUESTS_CA_BUNDLE handling and NFR4 idempotent skip-if-cached per-day/service fetching
- (decision) Cancellation fallback (FR12) lives inside the engine as a pure code path gated behind a flag until OQ1 resolved; hsp_client only surfaces raw late_canc_reason/empty-actuals signal
- (decision) Paradigm = hexagonal (ports & adapters): pure engine core, I/O adapters behind ports
- (decision) Engine = pure function core: engine.optimise(services, config)->claims, imports nothing. Composition root (cli.py now, Lambda handler later) fetches+writes and calls it. Prevents: I/O leaking into domain; keeps CLI and Lambda as thin interchangeable roots.
- (decision) Module boundaries: trainline/{engine/{models,delay,optimiser}, adapters/{hsp_client,storage,config}, cli.py}. Dependency direction inward: engine imports nothing; adapters import engine.models; cli imports all.
- (decision) HSP JSON->domain mapping happens inside hsp_client; engine never sees raw HSP shapes. engine.models is the single source of the Service/Claim shape. Prevents: HSP field names bleeding into the core / two divergent data shapes.
- (decision) Fetch failure = track & report distinctly: adapter records per-day/service fetch status; run reports 'fetch failed - not analysed' distinct from 'no claim'; a failed day never emitted as clean no-claim; no abort on flakiness. Prevents: silent missed claims (SM2/CM1).
- (decision) engine.models: Service{direction,date(ISO),rid,origin,destination,scheduled_departure,scheduled_arrival,actual_departure,actual_arrival,cancelled,reason}; Claim{date,direction,origin,destination,scheduled_departure,scheduled_arrival,actual_arrival,delay_min,band,payout,reason}; DayResult{claims[0..2], fetch_status}. delay_min/band/payout derived in engine.delay, not stored.
- (decision) Times normalised to integer minutes-since-midnight by hsp_client adapter (engine does clean int arithmetic); adapter owns HHMM parse and cross-midnight care for late-evening inbound.
- (constraint) Inherited from PRD: hexagonal pure core = FR11; cancellation gated = FR12/OQ1; NFR3 TLS, NFR4 idempotent cache, NFR6 Python; FR20 old tests rewritten not kept
- (decision) Reviewer fixes: AD-1 returns list[DayResult]; AD-4 origin-day-relative minutes + calculate_delay sole owner; AD-3 actual times int|None with skip rule; AD-5 day OK only if all legs fetched; AD-6 actual-late-beats-cancellation restored; added AD-10 deterministic/total optimiser (+optional cap for OQ2); added AD-11 Band.NONE=0/payout contract
- (decision) Conventions: hsp_client HTTP is injectable seam (NFR2 offline-first); output sorted date->direction (FR15). Stack bumped Python 3.10+, requests>=2.31 (3.8 EOL, current deps need >=3.9/3.10).
- (change) Updated source PRD to fix tie-break inconsistency I introduced: PRD §4 + addendum now match FR10 three-key rule (fewer rows, earliest outbound, earliest inbound)
- (event) Reviewer gate complete: lint 0, rubric PASS-WITH-FIXES, versions 1 flag, reconcile + adversarial applied. Deferred: OQ1 (cancellation fixture), OQ2 (cap value).
- (event) spine finalized
- (decision) AD-12 added (Update): test-first TDD per story — acceptance criteria encoded as pytest-bdd Gherkin scenarios written before code (fail-first then pass), engine logic TDD-unit-driven; story not done until acceptance tests green and no criterion untested. Binds every story; shapes bmad-create-epics-and-stories.
- (event) spine updated (AD-12 TDD) and re-finalized; lint 0
