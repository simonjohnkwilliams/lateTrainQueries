"""Per-day max-payout optimiser — sole owner of feasibility + tie-break (AD-10).

Stub for Story 1.1 (scaffold). ``optimise(days, config) -> list[DayResult]`` and
the gated cancellation path (AD-6) are implemented in Stories 1.4/1.5. Imports
nothing I/O (AD-1).
"""
