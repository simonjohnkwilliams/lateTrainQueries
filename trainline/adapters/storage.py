"""Storage adapter — Claim → CSV + JSON, sorted (FR13–FR15).

Stub for Story 1.1 (scaffold). Implemented in Story 3.2. Claim → CSV/JSON
serialisation happens only here (AD-3). May import ``trainline.engine.models``
only (AD-2).
"""
