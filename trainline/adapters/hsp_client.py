"""HSP API adapter — auth, AVG-TLS, injectable transport, cache; JSON → Service.

Stub for Story 1.1 (scaffold). Implemented across Epic 2 (Stories 2.1–2.5).
Re-implements NFR3 (REQUESTS_CA_BUNDLE) and NFR4 (response cache) fresh (AD-7).
May import ``trainline.engine.models`` only (AD-2).
"""
