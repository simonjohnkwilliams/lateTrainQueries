"""Config adapter — route/window/dates + credential loading (FR16, FR17).

Stub for Story 1.1 (scaffold). Implemented in Story 3.1. Config is passed
explicitly (no global state); credentials only from ``HSP_CREDENTIALS_FILE``.
May import ``trainline.engine.models`` only (AD-2).
"""
